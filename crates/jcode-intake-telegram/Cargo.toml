[package]
name = "jcode-intake-telegram"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
chrono = { version = "0.4", features = ["serde"] }
jcode-intake-types = { path = "../jcode-intake-types" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
